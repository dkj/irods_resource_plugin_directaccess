iRODS Direct Access Resource Plugin
-----------------------------------

To build the Direct Access Resource Plugin, you will need to have:

 - the iRODS Development Tools (irods-dev) installed for your platform
     http://eirods.org/download


Then to build this resource plugin:

  ./packaging/build.sh


To use this resource plugin:

  irods@hostname $ iadmin mkresc daResc directaccess <hostname>:<full/path/to/Vault>

